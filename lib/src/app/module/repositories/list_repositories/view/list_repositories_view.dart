import 'package:flutter/material.dart';

class ListRepositoriesView extends StatelessWidget {
  const ListRepositoriesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
